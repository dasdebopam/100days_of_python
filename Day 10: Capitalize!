'''
Question:
You are asked to ensure that the first and last names of people begin with a capital letter in their passports. For example, alison heck should be capitalised correctly as Alison Heck.

input:
chris alan

output:
Chris Alan

'''
def solve(s):
    s=s.split(" ")
    t=[]
    for i in s:
        i=i.capitalize()
        t.append(i)
        t.append(" ")
    u=''
    return u.join(t)
    
        

if __name__ == '__main__':
    fptr = open(os.environ['OUTPUT_PATH'], 'w')

    s = input()

    result = solve(s)

    fptr.write(result + '\n')

    fptr.close()
'''
thought: quite simple program 
all element in this were previously visited through other programs

'''
